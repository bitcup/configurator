# ------------------------------------------------------------------------------------
# this is an example of a registry file with a few servers
# ------------------------------------------------------------------------------------

# host: server 1 - has a config path (usually outside classpath) where FileConfig instances can be loaded from
server-1.type=app
server-1.configPath=/tmp/conf

# server 2
server-2.type=batch
